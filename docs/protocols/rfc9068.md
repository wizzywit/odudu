# RFC 9068 — JWT Profile for OAuth 2.0 Access Tokens

**Status in Odudu:** in scope for P1 in full. Odudu's design (design spec
§2.5) already commits to this profile: access tokens are JWTs typed
`at+jwt`, signed by the realm key, verifiable offline against `/jwks`;
refresh tokens stay opaque. §1 (Introduction) is non-normative and out of
scope. §2.2.1 (Authentication Information Claims — `auth_time`, `acr`,
`amr`) and §2.2.2 (Identity Claims) are OPTIONAL and get no rows, for the
same reason `oidc-discovery.md` gives OPTIONAL metadata: the level column
has no slot for it, and a row would not trace a requirement. §2.2.3
(Authorization Claims — `scope`, `groups`/`roles`/`entitlements`) has two
rows for the MUST and SHOULD claims on `scope`; naming `groups`, `roles`
and `entitlements` at all (§2.2.3.1) is OPTIONAL and gets no row, but its
one SHOULD — encoding their values per RFC 7643 — is not optional to trace
and gets the row below. §7 (IANA
Considerations) registers the media type and claim names; it imposes
nothing on an implementation and is out of scope.

## Reading notes

### The `resource` parameter and audience configuration

The design spec's "audience configuration" sits alongside consent and
dynamic client registration — admin-facing provisioning features, about how
audiences get configured on a client. The `resource` parameter is something
different: a request-time input to the authorization code grant. P1 shipped
the grant without reading it — a client sent `resource`, Odudu ignored it,
and `aud` was populated from the client's configured audiences instead — and
that deviation stayed a visible `gap` rather than a silent one until P3b
closed it. `/authorize` resolves and validates `resource`
(`parseResource`, `packages/protocol-oidc/src/service/resource-indicator.ts`)
and stores the result on the code; `/token` now derives `aud` from that
stored value, narrowed further by a `resource` the token request itself
names, and never widened past it (`resolveAudience`,
`packages/protocol-oidc/src/usecase/token-issuance.ts`) — the same function,
reused against the client's own configured `audiences`, decides a
`client_credentials` grant's `aud`, which has no code to narrow. §3's row
about honouring a supplied `resource` parameter closes below against that
mechanism. Inferring a default resource indicator from `scope`, and
refusing a `scope` that names several of them, are not attempted — nothing
in this server's scope model names a resource at all — and those two rows
stay `gap`.

**The default resource indicator itself is a different matter, and was held
before `resource` existed anywhere in this server.** §3's "if the
authorization request includes no `resource` parameter, the authorization
server uses a default resource indicator" requires `aud` to be populated
from somewhere other than the request when the request names nothing, and
it is: `resolveAudience` returns its `base` argument verbatim when no
`resource` narrows it, and that `base` is the client's configured
`audiences` at `/authorize` and at a codeless `client_credentials` grant, or
the code's own stored `resource` (or, on a refresh, the rotated grant's
`audience`) everywhere else — never re-read from `config.audiences` after a
`resource` has already narrowed it once. `RFC6750-5.3-02` asserts the
resulting `aud` value byte-for-byte, for the no-`resource`-anywhere case,
rather than merely asserting the claim is present; the row stays closed
against that test.

**§3's ambiguity MUST closes now, against that same mechanism, rather than
staying `deferred` — §2.2.3's does not, and the two are not one row.** RFC
9068 §3 names its own way to satisfy its MUST: "one way to accomplish this
is through the use of the `resource` parameter as defined in [RFC8707]."
§5's own example of what "ambiguous" means is a request naming several
resource indicators at once, and this server refuses that outright — two
`resource` values, or one the code's stored `resource` does not carry,
both refuse `invalid_target` rather than being resolved into a token
covering more than one audience the request actually asked for. Deleting
that refusal (`resolveAudience`'s `invalidTarget()` throw) fails
`RFC9068-3-03`'s own tests, which is what closes the row. The bare
`covered` on that row does not, on its own, say what §3 still permits: a
request naming **no** `resource` still gets `aud` restricted to the
client's whole configured `audiences` verbatim — §5's own ambiguity shape,
issued deliberately, exactly as `RFC6750-5.3-02`'s row above already
covers. The status column's grammar carries no room for that residual
inline (`covered` is a literal, not free text) — recorded here instead,
which is what a reader checking why the row closed has to consult anyway.

§2.2.3's "every scope string has meaning for the resources identified in
`aud`" is a different requirement that happens to sit next to §3's, and
nothing in this server checks it: no path in `token-issuance.ts`,
`resource-indicator.ts` or the scope services correlates a scope string
with an audience, narrowed or not — a client granted `openid` against an
`aud` naming an external resource is not asked whether `openid` means
anything there, and it is issued regardless. `RFC9068-3-03`'s tests are
all about `invalid_target` refusal and narrowing, and none of them would
notice `openid` failing to mean anything for `https://reports.example`;
citing `RFC9068-3-03` here would be the failure mode the brief warned
about — a check exists, but it is not the check this row is about. The row
is `accepted:` instead, against "Why §2.2.3 is accepted, not held" below.

### Why §2.2.3 is accepted, not held

Nothing in this server ties a scope string to an audience, narrowed or
not — `scope` is resolved once, against the client's assigned scopes
(`resolveScope`), and reaches the token whole regardless of what `aud`
ended up being. RFC 8707 gives a client the means to narrow `aud` to one
resource, but narrowing `aud` says nothing about whether the granted scope
means anything there; a client could as easily request `reports:read`
against `resource=https://api.example`, and nothing here would object.
Building the correlation this MUST asks for needs a per-audience scope
model — which resources a scope is meaningful for — that this server does
not have; RFC 8707 narrowed _what_ a token is restricted to, not _whether_
its scope suits that restriction. Closing this honestly is client
management's, not this task's: recorded as an open question for whichever
phase gives scopes an audience of their own, rather than claimed here
against a mechanism that does not establish it.

### Token-type confusion, and what `typ: at+jwt` closes

An OAuth access token and an OpenID Connect ID Token can end up looking
identical to a naive verifier: both are JWTs, both are signed by the same
authorization server, both can carry the same `iss` and pass signature
verification against the same `/jwks`. If a resource server's check is
"a JWT, signed by an issuer I trust," nothing in that check distinguishes
an access token minted for calling this API from an ID Token minted to
tell some client who the user is. A client (or an attacker who obtained
an ID Token some other way — it is disclosed to the client by design)
could hand the resource server an ID Token, and if the resource server
never checks the token's declared purpose, it would validate.
Token-type confusion is exactly this: two token kinds sharing a
representation and an issuer, distinguished by nothing but out-of-band
assumption.

`typ: at+jwt` in the JWT header closes this by making purpose part of
what gets verified, not just claims content. §4 requires the resource
server to check the header's `typ` before it does anything else with the
token; an ID Token — which per OpenID Connect Core carries no `at+jwt`
`typ` value — fails this check immediately, before its `iss`, `aud`, or
`exp` are ever consulted. The defense holds because it is cheap and
mandatory to check first, not because it is hard to forge: a party that
already controls token issuance could set any `typ` it likes. What it
protects against is a resource server built to the letter of "verify
signature, check claims" but missing the type check, since that omission
is exactly the kind of thing that is invisible in code review and only
shows up against an adversarial corpus — which is why Odudu's own such
corpus (design spec §5) includes both `alg: none` and cross-token-type
substitution among its cases.

### §2.2.3.1: `roles`, `groups` and `entitlements` against RFC 7643's schema

All three are IANA-registered JWT claims (RFC 7643 §4.1.2, referenced by
RFC 9068 §2.2.3.1), not something this project invented. §2.2.3.1 also
SHOULDs that their values be encoded "according to the guidance defined
in [RFC7643]" — Odudu does not: `groups` is emitted as a flat array of
strings rather than RFC 7643's complex multi-valued form (`value`, `$ref`,
`display`, `type` sub-attributes), because Kubernetes reads the claim
named by `--oidc-groups-claim` as a list of strings even for a single
value, and ArgoCD, Grafana and Vault all assume the same shape. Recorded
here as a deliberate divergence, the way ADR 0022 records one for this
same claim's membership scope. `entitlements` is not emitted at all —
Odudu has no entitlement concept.

RFC 7643 §4.1.2 does carry MUST/SHOULD text of its own (the `$ref` and
mutability rules for `groups`, canonicalization for `emails`/
`phoneNumbers`, the `photos` MUST), but every one of them binds a SCIM
service provider managing these as SCIM resources. Odudu implements no
SCIM interface, so none of it applies here, and a `docs/protocols/rfc7643.md`
table would be a page of `n/a: no SCIM interface` rows tracing nothing. No
table.

## Clause table

| Clause  | Level  | Requirement                                                                                                                                                                            | Test ID            | Status                                                                                                                                                                                                                                                                                |
| ------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.1     | MUST   | a JWT access token is signed                                                                                                                                                           | `RFC9068-2.1-02`   | covered                                                                                                                                                                                                                                                                               |
| 2.1     | MUST   | a JWT access token does not use `none` as its signing algorithm                                                                                                                        | `RFC9068-2.1-03`   | covered                                                                                                                                                                                                                                                                               |
| 2.1     | MUST   | authorization servers and resource servers include RS256 among their supported signature algorithms                                                                                    | `RFC9068-2.1-05`   | covered                                                                                                                                                                                                                                                                               |
| 2.1     | MUST   | a JWT access token includes the `application/at+jwt` media type in its `typ` header parameter                                                                                          | `RFC9068-2.1-04`   | covered                                                                                                                                                                                                                                                                               |
| 2.1     | SHOULD | the `typ` value used is `at+jwt` rather than `application/at+jwt`                                                                                                                      | `RFC9068-2.1-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.1     | SHOULD | a JWT access token is signed with an asymmetric algorithm rather than a symmetric one                                                                                                  | `RFC9068-2.1-03`   | covered                                                                                                                                                                                                                                                                               |
| 2.2     | MUST   | the JWT access token includes an `iss` claim                                                                                                                                           | `RFC9068-2.2-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.2     | MUST   | the JWT access token includes an `exp` claim                                                                                                                                           | `RFC9068-2.2-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.2     | MUST   | the JWT access token includes an `aud` claim                                                                                                                                           | `RFC9068-2.2-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.2     | MUST   | the JWT access token includes a `sub` claim                                                                                                                                            | `RFC9068-2.2-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.2     | SHOULD | for grants involving a resource owner, `sub` corresponds to the resource owner's subject identifier                                                                                    | —                  | gap                                                                                                                                                                                                                                                                                   |
| 2.2     | SHOULD | for grants with no resource owner (`client_credentials`), `sub` corresponds to an identifier the authorization server uses for the client application                                  | —                  | gap                                                                                                                                                                                                                                                                                   |
| 2.2     | MUST   | the JWT access token includes a `client_id` claim                                                                                                                                      | `RFC9068-2.2-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.2     | MUST   | the JWT access token includes an `iat` claim                                                                                                                                           | `RFC9068-2.2-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.2     | MUST   | the JWT access token includes a `jti` claim                                                                                                                                            | `RFC9068-2.2-01`   | covered                                                                                                                                                                                                                                                                               |
| 2.2.3   | MUST   | every scope string in the `scope` claim has meaning for the resources identified in the `aud` claim                                                                                    | —                  | accepted: "Why §2.2.3 is accepted, not held" — no per-audience scope model exists to correlate them                                                                                                                                                                                   |
| 2.2.3   | SHOULD | if the authorization request includes a `scope` parameter, the corresponding JWT access token includes a `scope` claim                                                                 | `RFC9068-2.2.3-01` | covered                                                                                                                                                                                                                                                                               |
| 2.2.3.1 | SHOULD | `groups`/`roles`/`entitlements` values are encoded according to the guidance defined in RFC 7643                                                                                       | —                  | accepted: "§2.2.3.1: `roles`, `groups` and `entitlements` against RFC 7643's schema" — `groups` is a flat string array, not RFC 7643's complex multi-valued form, because that is what Kubernetes, ArgoCD, Grafana and Vault all read                                                 |
| 3       | MUST   | the authorization server does not issue a JWT access token if the authorization it grants would be ambiguous                                                                           | `RFC9068-3-03`     | covered                                                                                                                                                                                                                                                                               |
| 3       | MUST   | if the authorization request includes no `resource` parameter, the authorization server uses a default resource indicator in the `aud` claim                                           | `RFC6750-5.3-02`   | covered                                                                                                                                                                                                                                                                               |
| 3       | SHOULD | if the request includes a `resource` parameter, the `aud` claim has the same value as the `resource` parameter                                                                         | `RFC8707-2-06`     | covered                                                                                                                                                                                                                                                                               |
| 3       | SHOULD | when a `scope` parameter is present, the authorization server uses it to infer the default resource indicator for the `aud` claim                                                      | —                  | gap                                                                                                                                                                                                                                                                                   |
| 3       | SHOULD | if the `scope` values reference different default resource indicators, the authorization server rejects the request with `invalid_scope`                                               | —                  | gap                                                                                                                                                                                                                                                                                   |
| 4       | SHOULD | authorization servers advertise their signing keys and expected `iss` value via `jwks_uri` and `issuer`, using OAuth 2.0 Authorization Server Metadata or OpenID Connect discovery     | —                  | gap                                                                                                                                                                                                                                                                                   |
| 4       | MUST   | if an authorization server supports both OAuth 2.0 Authorization Server Metadata and OpenID Connect discovery, the values provided are consistent across both                          | —                  | n/a: Odudu publishes one discovery document; there is no second metadata surface to stay consistent with                                                                                                                                                                              |
| 4       | MUST   | the resource server verifies that the `typ` header value is `at+jwt` or `application/at+jwt`, rejecting tokens carrying any other value                                                | `RFC9068-4-01`     | covered                                                                                                                                                                                                                                                                               |
| 4       | MUST   | if the JWT access token is encrypted, the resource server decrypts it using the keys and algorithms specified during registration                                                      | —                  | n/a: Odudu never issues encrypted (JWE) access tokens — per design spec §2.5 they are always signed `at+jwt`, and no encryption is ever negotiated at registration                                                                                                                    |
| 4       | SHOULD | if encryption was negotiated with the authorization server at registration and an incoming JWT access token is not encrypted, the resource server rejects it                           | —                  | n/a: same reasoning — Odudu never negotiates access-token encryption at registration                                                                                                                                                                                                  |
| 4       | MUST   | the resource server matches the `iss` claim against the issuer identifier it obtained (typically via discovery)                                                                        | `RFC9068-4-02`     | covered                                                                                                                                                                                                                                                                               |
| 4       | MUST   | the resource server validates that the `aud` claim contains a resource indicator identifying itself, rejecting the token if it does not                                                | `RFC9068-4-05`     | covered                                                                                                                                                                                                                                                                               |
| 4       | MUST   | the resource server validates the JWT signature per RFC 7515, using the algorithm named in the `alg` header parameter                                                                  | `RFC9068-4-03`     | covered                                                                                                                                                                                                                                                                               |
| 4       | MUST   | the resource server rejects any JWT whose `alg` value is `none`                                                                                                                        | `RFC9068-4-06`     | covered                                                                                                                                                                                                                                                                               |
| 4       | MUST   | the resource server uses the keys provided by the authorization server to validate the signature                                                                                       | `RFC9068-4-07`     | covered                                                                                                                                                                                                                                                                               |
| 4       | MUST   | the current time is before the time represented by the `exp` claim                                                                                                                     | `RFC9068-4-04`     | covered                                                                                                                                                                                                                                                                               |
| 4       | MUST   | the resource server handles validation failures per RFC 6750 §3.1, returning the `invalid_token` error code                                                                            | `RFC6750-3.1-01`   | covered                                                                                                                                                                                                                                                                               |
| 4       | SHOULD | if the JWT access token includes authorization claims (§2.2.3), the resource server uses them in combination with other contextual information to decide whether to authorize the call | —                  | n/a: this obliges a resource server's authorization decision; Odudu issues these claims as the authorization server, and its one resource-server role (`/userinfo`, §6) decides admission by bearer-token and scope validation, never by reading `groups`/`roles`/`entitlements` back |
| 5       | MUST   | authorization servers use a distinct `aud` claim value to uniquely identify access tokens issued by the same issuer for distinct resources                                             | `RFC9068-5-01`     | covered                                                                                                                                                                                                                                                                               |
| 6       | MUST   | the client does not inspect the content of the access token                                                                                                                            | —                  | n/a: client-side guidance; Odudu is the authorization server and, for the userinfo endpoint, the resource server, not a client consuming its own access tokens                                                                                                                        |
