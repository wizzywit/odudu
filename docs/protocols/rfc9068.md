# RFC 9068 — JWT Profile for OAuth 2.0 Access Tokens

**Status in Odudu:** in scope for P1 in full. Odudu's design (design spec
§2.5) already commits to this profile: access tokens are JWTs typed
`at+jwt`, signed by the realm key, verifiable offline against `/jwks`;
refresh tokens stay opaque. §1 (Introduction) is non-normative and out of
scope. §2.2.1 (Authentication Information Claims — `auth_time`, `acr`,
`amr`) and §2.2.2 (Identity Claims) are OPTIONAL and get no rows, for the
same reason `oidc-discovery.md` gives OPTIONAL metadata: the level column
has no slot for it, and a row would not trace a requirement. §2.2.3
(Authorization Claims — `scope`, `groups`/`roles`/`entitlements`) has two
rows for the MUST and SHOULD claims on `scope`; the guidance on groups,
roles and entitlements (§2.2.3.1) is OPTIONAL and gets no rows. §7 (IANA
Considerations) registers the media type and claim names; it imposes
nothing on an implementation and is out of scope.

## Reading notes

### The `resource` parameter and audience configuration

Section 11's "audience configuration" sits alongside consent and dynamic client registration — those are admin-facing provisioning features, about how audiences get configured on a client. The `resource` parameter is something different: a request-time input to the authorization code grant that P1 already ships. Deferring it hides a real deviation in a flow that is supposedly finished — a client sends `resource`, Odudu ignores it, and `aud` is populated from the default resource indicator instead. As a `gap` it stays visible and forces an explicit decision at Task 18, where the honest resolution may well be that Odudu does not implement RFC 8707 and says so. That decision should be made in the open rather than absorbed by a deferral.

### Token-type confusion, and what `typ: at+jwt` closes

An OAuth access token and an OpenID Connect ID Token can end up looking
identical to a naive verifier: both are JWTs, both are signed by the same
authorization server, both can carry the same `iss` and pass signature
verification against the same `/jwks`. If a resource server's check is
"a JWT, signed by an issuer I trust," nothing in that check distinguishes
an access token minted for calling this API from an ID Token minted to
tell some client who the user is. A client (or an attacker who obtained
an ID Token some other way — it is disclosed to the client by design)
could hand the resource server an ID Token, and if the resource server
never checks the token's declared purpose, it would validate.
Token-type confusion is exactly this: two token kinds sharing a
representation and an issuer, distinguished by nothing but out-of-band
assumption.

`typ: at+jwt` in the JWT header closes this by making purpose part of
what gets verified, not just claims content. §4 requires the resource
server to check the header's `typ` before it does anything else with the
token; an ID Token — which per OpenID Connect Core carries no `at+jwt`
`typ` value — fails this check immediately, before its `iss`, `aud`, or
`exp` are ever consulted. The defense holds because it is cheap and
mandatory to check first, not because it is hard to forge: a party that
already controls token issuance could set any `typ` it likes. What it
protects against is a resource server built to the letter of "verify
signature, check claims" but missing the type check, since that omission
is exactly the kind of thing that is invisible in code review and only
shows up against an adversarial corpus — which is why Odudu's own such
corpus (design spec §5) includes both `alg: none` and cross-token-type
substitution among its cases.

## Clause table

| Clause | Level  | Requirement                                                                                                                                                                            | Test ID          | Status                                                                                                                                                             |
| ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2.1    | MUST   | a JWT access token is signed                                                                                                                                                           | —                | gap                                                                                                                                                                |
| 2.1    | MUST   | a JWT access token does not use `none` as its signing algorithm                                                                                                                        | —                | gap                                                                                                                                                                |
| 2.1    | MUST   | authorization servers and resource servers include RS256 among their supported signature algorithms                                                                                    | —                | gap                                                                                                                                                                |
| 2.1    | MUST   | a JWT access token includes the `application/at+jwt` media type in its `typ` header parameter                                                                                          | —                | gap                                                                                                                                                                |
| 2.1    | SHOULD | the `typ` value used is `at+jwt` rather than `application/at+jwt`                                                                                                                      | `RFC9068-2.1-01` | covered                                                                                                                                                            |
| 2.1    | SHOULD | a JWT access token is signed with an asymmetric algorithm rather than a symmetric one                                                                                                  | —                | gap                                                                                                                                                                |
| 2.2    | MUST   | the JWT access token includes an `iss` claim                                                                                                                                           | `RFC9068-2.2-01` | covered                                                                                                                                                            |
| 2.2    | MUST   | the JWT access token includes an `exp` claim                                                                                                                                           | `RFC9068-2.2-01` | covered                                                                                                                                                            |
| 2.2    | MUST   | the JWT access token includes an `aud` claim                                                                                                                                           | `RFC9068-2.2-01` | covered                                                                                                                                                            |
| 2.2    | MUST   | the JWT access token includes a `sub` claim                                                                                                                                            | `RFC9068-2.2-01` | covered                                                                                                                                                            |
| 2.2    | SHOULD | for grants involving a resource owner, `sub` corresponds to the resource owner's subject identifier                                                                                    | —                | gap                                                                                                                                                                |
| 2.2    | SHOULD | for grants with no resource owner (`client_credentials`), `sub` corresponds to an identifier the authorization server uses for the client application                                  | —                | gap                                                                                                                                                                |
| 2.2    | MUST   | the JWT access token includes a `client_id` claim                                                                                                                                      | `RFC9068-2.2-01` | covered                                                                                                                                                            |
| 2.2    | MUST   | the JWT access token includes an `iat` claim                                                                                                                                           | `RFC9068-2.2-01` | covered                                                                                                                                                            |
| 2.2    | MUST   | the JWT access token includes a `jti` claim                                                                                                                                            | `RFC9068-2.2-01` | covered                                                                                                                                                            |
| 2.2.3  | MUST   | every scope string in the `scope` claim has meaning for the resources identified in the `aud` claim                                                                                    | —                | gap                                                                                                                                                                |
| 2.2.3  | SHOULD | if the authorization request includes a `scope` parameter, the corresponding JWT access token includes a `scope` claim                                                                 | —                | gap                                                                                                                                                                |
| 3      | MUST   | the authorization server does not issue a JWT access token if the authorization it grants would be ambiguous                                                                           | —                | gap                                                                                                                                                                |
| 3      | MUST   | if the authorization request includes no `resource` parameter, the authorization server uses a default resource indicator in the `aud` claim                                           | —                | gap                                                                                                                                                                |
| 3      | SHOULD | if the request includes a `resource` parameter, the `aud` claim has the same value as the `resource` parameter                                                                         | —                | gap                                                                                                                                                                |
| 3      | SHOULD | when a `scope` parameter is present, the authorization server uses it to infer the default resource indicator for the `aud` claim                                                      | —                | gap                                                                                                                                                                |
| 3      | SHOULD | if the `scope` values reference different default resource indicators, the authorization server rejects the request with `invalid_scope`                                               | —                | gap                                                                                                                                                                |
| 4      | SHOULD | authorization servers advertise their signing keys and expected `iss` value via `jwks_uri` and `issuer`, using OAuth 2.0 Authorization Server Metadata or OpenID Connect discovery     | —                | gap                                                                                                                                                                |
| 4      | MUST   | if an authorization server supports both OAuth 2.0 Authorization Server Metadata and OpenID Connect discovery, the values provided are consistent across both                          | —                | n/a: Odudu publishes one discovery document; there is no second metadata surface to stay consistent with                                                           |
| 4      | MUST   | the resource server verifies that the `typ` header value is `at+jwt` or `application/at+jwt`, rejecting tokens carrying any other value                                                | `RFC9068-4-01`   | covered                                                                                                                                                            |
| 4      | MUST   | if the JWT access token is encrypted, the resource server decrypts it using the keys and algorithms specified during registration                                                      | —                | n/a: Odudu never issues encrypted (JWE) access tokens — per design spec §2.5 they are always signed `at+jwt`, and no encryption is ever negotiated at registration |
| 4      | SHOULD | if encryption was negotiated with the authorization server at registration and an incoming JWT access token is not encrypted, the resource server rejects it                           | —                | n/a: same reasoning — Odudu never negotiates access-token encryption at registration                                                                               |
| 4      | MUST   | the resource server matches the `iss` claim against the issuer identifier it obtained (typically via discovery)                                                                        | `RFC9068-4-02`   | covered                                                                                                                                                            |
| 4      | MUST   | the resource server validates that the `aud` claim contains a resource indicator identifying itself, rejecting the token if it does not                                                | `RFC9068-4-05`   | covered                                                                                                                                                            |
| 4      | MUST   | the resource server validates the JWT signature per RFC 7515, using the algorithm named in the `alg` header parameter                                                                  | `RFC9068-4-03`   | covered                                                                                                                                                            |
| 4      | MUST   | the resource server rejects any JWT whose `alg` value is `none`                                                                                                                        | —                | gap                                                                                                                                                                |
| 4      | MUST   | the resource server uses the keys provided by the authorization server to validate the signature                                                                                       | —                | gap                                                                                                                                                                |
| 4      | MUST   | the current time is before the time represented by the `exp` claim                                                                                                                     | `RFC9068-4-04`   | covered                                                                                                                                                            |
| 4      | MUST   | the resource server handles validation failures per RFC 6750 §3.1, returning the `invalid_token` error code                                                                            | `RFC6750-3.1-01` | covered                                                                                                                                                            |
| 4      | SHOULD | if the JWT access token includes authorization claims (§2.2.3), the resource server uses them in combination with other contextual information to decide whether to authorize the call | —                | n/a: the `groups`/`roles`/`entitlements` claims of §2.2.3.1 are OPTIONAL and not emitted by Odudu in P1, so this guidance has no claims to act on                  |
| 5      | MUST   | authorization servers use a distinct `aud` claim value to uniquely identify access tokens issued by the same issuer for distinct resources                                             | `RFC9068-5-01`   | covered                                                                                                                                                            |
| 6      | MUST   | the client does not inspect the content of the access token                                                                                                                            | —                | n/a: client-side guidance; Odudu is the authorization server and, for the userinfo endpoint, the resource server, not a client consuming its own access tokens     |
