# RFC 8707 — Resource Indicators for OAuth 2.0

**Status in Odudu:** in scope for P3b, per the design spec's §8.1: the
`resource` parameter is accepted at `/authorize` and `/token`,
single-valued, validated against the client's configured audience
allowlist, making `aud` derived rather than asserted. `/authorize` resolves
and validates it (`parseResource`,
`packages/protocol-oidc/src/service/resource-indicator.ts`) and stores the
result on the code; `/token` now reads it too, narrowing what the code (or,
on a refresh, the grant it rotated) carries, or deciding a codeless
`client_credentials` grant's audience against the client's own configured
list (`resolveAudience`,
`packages/protocol-oidc/src/usecase/token-issuance.ts`) — see
`docs/request-paths.md`'s `/token` entry for a transcript. §1
(Introduction), §1.1 (Requirements Notation and Conventions) and §1.2
(Terminology) are non-normative and out of scope; §1.1 is also the reason
every lowercase "must"/"should" elsewhere in this document — §2.1's and
§2.2's restatements of already-normative obligations, both discussed below
— carries no row, since §1.1 states plainly that only the all-capitals
form is normative here. §3 (Security Considerations) and §4 (Privacy
Considerations) contain no capitalized RFC 2119 keyword and are not rowed.
§5 (IANA Considerations) registers the parameter and error code
themselves, not a value Odudu would add to either registry, and is not
rowed.

## Reading notes

### `resource` stays single-valued: declined, not absent

§2's MAY that multiple `resource` parameters indicate a token intended for
several resources is not planned. The design spec's decision is a single
`resource` value, checked against one client's own audience allowlist —
the multi-resource form trades that for a token whose bearer can present
it at any of several resources interchangeably, which RFC 8707 §3 itself
flags as needing "a high degree of trust between the involved parties."
Nothing in Odudu's client or audience model establishes that trust between
two independently registered resources, so the row below is `accepted:`
against this note rather than left open.

### A query component: the allowlist refuses it, the parser does not

§2's SHOULD that a `resource` value carry no query component is addressed
to whoever constructs the value — the same reading the two client-side
SHOULDs below it get — and `parseResource`
(`packages/protocol-oidc/src/service/resource-indicator.ts`) does not check
for one. What bounds the value instead is stronger than a syntactic test:
an explicit `resource` is accepted only when it matches an entry the
operator registered in that client's `audiences`, byte for byte, so the
only query-bearing value the server can ever accept is one the deployment
wrote down itself. Enforcing the SHOULD at `/authorize` would refuse a
registered audience an operator deliberately chose, which is a worse
failure than the one it prevents. Accepted rather than left open.

### The omitted-`resource` MAY, already covered elsewhere

§2.1's MAY — that an authorization server either processes a request that
omits `resource` using a predefined default, or instead requires the
parameter and fails omission with `invalid_target` — names two branches.
Odudu takes the first: `resolveAudience`
(`packages/protocol-oidc/src/usecase/token-issuance.ts`) returns its `base`
verbatim when no resource indicator narrows it, whether `base` is the
client's configured `audiences` or a code's (or grant's) already-resolved
value, which is `docs/protocols/rfc9068.md` §3's row, closed against
`RFC6750-5.3-02`. Rowing the same choice again here would double-count an
obligation already closed under a different RFC's clause number, the way
`docs/protocols/oidc-frontchannel.md` declines to re-row the `sid` Claim's
definition once `oidc-backchannel.md` has closed it. Not rowed here.

### §2.2's refresh example, and where this server diverges from it

§2.2 (non-normative — lowercase "must", out of §1.1's scope for a row) says
a `refresh_token` or `authorization_code` request's acceptable resources
are "at [the authorization server's] sole discretion", and that in the
`authorization_code` case such policy "may limit the acceptable resources
to those that were originally granted by the resource owner **or a subset
thereof**". Its own Figures 5–6 walk a refresh asking for
`https://contacts.example.com/` after the original code redemption (Figure 3) asked for `https://cal.example.com/` — a _different_ resource than what
the access token was narrowed to, which §2.2 permits because "any refresh
token that is returned is bound to the **full original grant**", not to
whatever the access token was narrowed to at redemption.

This server does not do that. `issueRefreshTokens`
(`packages/protocol-oidc/src/usecase/token-issuance.ts`) derives its
ceiling from `grant.audience` — the audience already narrowed and stored
at the `authorization_code` redemption that created the grant, never
re-widened to whatever the client was originally entitled to request. §2.2's
Figure 5 example, replayed against this server, would refuse: `resource`
naming a resource that redemption's own `resource` didn't carry is
`invalid_target`, exactly like a code redemption. §2.2's "sole discretion
… or a subset thereof" permits narrower-than-full-grant policy, so this is
conforming — but it is a real choice, not a default: a refresh that could
re-request anything the _original_ grant covered would let a client
narrow once at redemption and get the wider audience back on any later
refresh, defeating "a `resource` may narrow and never widen" in one round
trip. Recorded here rather than left for a reader to discover only by
trying Figure 5's own request.

One asymmetry this produces, worth naming plainly: `grant.audience` always
carries the issuer (`mintAccessToken` appends it before the grant is
written), while a code's own stored `resource` does not. So
`resource=<this realm's issuer>` at a refresh is accepted (the issuer is
already a member of `grant.audience`) where the identical value at a code
redemption is refused `invalid_target` (the issuer is never a member of
`code.resource`). Harmless — the issuer is appended to `aud` either way,
narrowed or not — but a client probing the two doors would see it.

### No discovery metadata to advertise

RFC 8707 registers no member of the OAuth 2.0 Authorization Server Metadata
or OpenID Connect discovery document — unlike, say, `resource_indicators`
being a thing a client could detect support for before sending one. A
client learns whether `resource` is honoured only by sending it and reading
`aud`, or by out-of-band knowledge of this server. `oidc-discovery.md`
carries no row for this RFC and gains none from this task; recorded here so
a reader does not go looking for one.

## Clause table

| Clause | Level  | Requirement                                                                                                                  | Test ID        | Status                                                                       |
| ------ | ------ | ---------------------------------------------------------------------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------- |
| 2      | MUST   | a `resource` parameter's value is an absolute URI as specified by RFC 3986 §4.3                                              | `RFC8707-2-04` | covered                                                                      |
| 2      | MUST   | a `resource` parameter's value includes no fragment component                                                                | `RFC8707-2-05` | covered                                                                      |
| 2      | SHOULD | a `resource` parameter's value does not include a query component                                                            | —              | accepted: "A query component: the allowlist refuses it, the parser does not" |
| 2      | MAY    | multiple `resource` parameters are used to indicate that the requested token is intended for use at multiple resources       | —              | accepted: "`resource` stays single-valued: declined, not absent"             |
| 2      | SHOULD | the client provides the most specific URI it can for the complete API or set of resources it intends to access               | —              | n/a: client-side guidance; Odudu is the authorization server, not a client   |
| 2      | SHOULD | the client uses the base URI of the API as the `resource` value unless specific knowledge of the resource dictates otherwise | —              | n/a: client-side guidance; Odudu is the authorization server, not a client   |
| 2      | SHOULD | the authorization server audience-restricts an issued access token to the resource(s) indicated by the `resource` parameter  | `RFC8707-2-06` | covered                                                                      |
