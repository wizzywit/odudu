# RFC 6750 — OAuth 2.0 Bearer Token Usage

**Status in Odudu:** in scope for P1. The userinfo endpoint accepts bearer
tokens via the `Authorization` header only — OAuth 2.1 drops the URI query
parameter method entirely, and Odudu does not implement the form-encoded
body method either, since userinfo is a `GET` endpoint with no request
body to carry a token in.

## Reading notes

RFC 6750 defines three ways a client can present a bearer token
(`Authorization` header, form-encoded body, URI query parameter) and one
way a resource server reports a failed request back (`WWW-Authenticate`
with a `Bearer` challenge and one of three error codes). Odudu is the
resource server for its own userinfo endpoint, so §3's `WWW-Authenticate`
requirements and §5's confidentiality requirements are the ones this
implementation is actually judged against; §2's three presentation methods
are mostly about what the _client_ does with the token it already has, and
only one of those three — the `Authorization` header — is one Odudu's
resource server needs to accept.

The URI query parameter method (§2.3) is the one place this table's `n/a`
rows come from a project decision rather than an OAuth-2.1-wide one: OAuth
2.1 drops it because a token in a URL query string ends up in server logs,
browser history, and `Referer` headers by default, and RFC 6750 itself
already downgrades it to "SHOULD NOT ... unless it is impossible" for the
same reason. Odudu never accepts it.

## Requirements

| Clause | Level  | Requirement                                                                                                                                                                     | Test ID | Status                                                                                                                         |
| ------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 2      | MUST   | a client uses no more than one method to transmit the bearer token in a single request                                                                                          | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 2.1    | SHOULD | a client sends the bearer token using the `Authorization` header field with the `Bearer` scheme                                                                                 | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 2.1    | MUST   | the resource server supports the `Authorization` header method for bearer tokens                                                                                                | —       | gap                                                                                                                            |
| 2.2    | MUST   | a client sending the token as a form-encoded body parameter meets all five stated preconditions (content type, encoding, single-part, ASCII-only, a method with body semantics) | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 2.2    | MUST   | `access_token` in a form-encoded body is separated from other parameters with `&`                                                                                               | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 2.2    | SHOULD | the form-encoded body method is used only where the `Authorization` header is unavailable to the client                                                                         | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 2.2    | MAY    | the resource server supports the form-encoded body method                                                                                                                       | —       | n/a: Odudu's userinfo endpoint is `GET`-only; the form-encoded body method requires a request body, which `GET` does not carry |
| 2.3    | MAY    | the resource server supports the URI query parameter method                                                                                                                     | —       | n/a: URI query parameter method dropped in OAuth 2.1; Odudu accepts the `Authorization` header only                            |
| 2.3    | SHOULD | a client using the URI query parameter method sends `Cache-Control: no-store`                                                                                                   | —       | n/a: URI query parameter method dropped in OAuth 2.1; Odudu accepts the `Authorization` header only                            |
| 2.3    | SHOULD | a server success response to a URI query parameter request carries `Cache-Control: private`                                                                                     | —       | n/a: URI query parameter method dropped in OAuth 2.1; Odudu accepts the `Authorization` header only                            |
| 2.3    | SHOULD | the URI query parameter method is avoided unless the header and body methods are both unavailable                                                                               | —       | n/a: URI query parameter method dropped in OAuth 2.1; Odudu accepts the `Authorization` header only                            |
| 3      | MUST   | the resource server includes `WWW-Authenticate` when a request lacks credentials or carries a token that does not grant access                                                  | —       | gap                                                                                                                            |
| 3      | MAY    | the resource server includes `WWW-Authenticate` under other conditions as well                                                                                                  | —       | gap                                                                                                                            |
| 3      | MUST   | every challenge defined by this specification uses the `Bearer` auth-scheme value                                                                                               | —       | gap                                                                                                                            |
| 3      | MUST   | the `Bearer` scheme is followed by one or more auth-param values                                                                                                                | —       | gap                                                                                                                            |
| 3      | MAY    | other auth-param attributes beyond `realm`, `scope`, `error`, `error_description`, `error_uri` are used                                                                         | —       | gap                                                                                                                            |
| 3      | MAY    | a `realm` attribute is included to indicate the scope of protection                                                                                                             | —       | gap                                                                                                                            |
| 3      | MUST   | `realm` appears at most once                                                                                                                                                    | —       | gap                                                                                                                            |
| 3      | MAY    | a `scope` attribute is included, as a space-delimited, order-insensitive list of case-sensitive values                                                                          | —       | gap                                                                                                                            |
| 3      | MUST   | `scope` appears at most once                                                                                                                                                    | —       | gap                                                                                                                            |
| 3      | SHOULD | the resource server includes the `error` attribute when the request carried a token that failed authentication                                                                  | —       | gap                                                                                                                            |
| 3      | MAY    | the resource server includes `error_description`                                                                                                                                | —       | gap                                                                                                                            |
| 3      | MAY    | the resource server includes `error_uri`, as an absolute URI                                                                                                                    | —       | gap                                                                                                                            |
| 3      | MUST   | `error`, `error_description`, and `error_uri` each appear at most once                                                                                                          | —       | gap                                                                                                                            |
| 3      | MUST   | `scope` values are restricted to the defined character set, with space as the delimiter                                                                                         | —       | gap                                                                                                                            |
| 3      | MUST   | `error` and `error_description` values are restricted to the defined printable-ASCII character set                                                                              | —       | gap                                                                                                                            |
| 3      | MUST   | `error_uri` conforms to URI-reference syntax and the defined character set                                                                                                      | —       | gap                                                                                                                            |
| 3.1    | SHOULD | the resource server responds with HTTP 400 for `invalid_request`                                                                                                                | —       | gap                                                                                                                            |
| 3.1    | SHOULD | the resource server responds with HTTP 401 for `invalid_token`                                                                                                                  | —       | gap                                                                                                                            |
| 3.1    | MAY    | the client requests a new access token and retries after `invalid_token`                                                                                                        | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 3.1    | SHOULD | the resource server responds with HTTP 403 for `insufficient_scope`                                                                                                             | —       | gap                                                                                                                            |
| 3.1    | MAY    | the resource server includes a `scope` attribute naming the scope needed, on `insufficient_scope`                                                                               | —       | gap                                                                                                                            |
| 3.1    | SHOULD | the resource server omits an error code when the request carried no authentication information at all                                                                           | —       | gap                                                                                                                            |
| 5.2    | MUST   | a bearer token used as a reference (rather than self-contained) is infeasible for an attacker to guess                                                                          | —       | gap                                                                                                                            |
| 5.2    | MUST   | token integrity protection is sufficient to prevent modification                                                                                                                | —       | gap                                                                                                                            |
| 5.2    | SHOULD | a token's use is restricted to a specific scope                                                                                                                                 | —       | gap                                                                                                                            |
| 5.2    | MUST   | the authorization server implements TLS                                                                                                                                         | —       | gap                                                                                                                            |
| 5.2    | MUST   | confidentiality protection via TLS is applied to client–authorization-server and client–resource-server interactions                                                            | —       | gap                                                                                                                            |
| 5.2    | MUST   | token encryption is applied in addition to TLS when the client itself must not observe the token's contents                                                                     | —       | gap                                                                                                                            |
| 5.2    | MUST   | the client validates the TLS certificate chain, including revocation status, when presenting a token                                                                            | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 5.2    | MUST   | bearer tokens are not stored in cookies sent in the clear                                                                                                                       | —       | gap                                                                                                                            |
| 5.2    | MUST   | where a TLS connection terminates before the resource-providing server, confidentiality of the token is maintained between them                                                 | —       | deferred: P4 — a property of the deployment topology behind the resource server, not the userinfo endpoint itself              |
| 5.2    | MUST   | token lifetime is limited                                                                                                                                                       | —       | gap                                                                                                                            |
| 5.2    | MUST   | confidentiality protection is applied to client–authorization-server and client–resource-server exchanges to prevent replay                                                     | —       | gap                                                                                                                            |
| 5.2    | MUST   | the client verifies the resource server's identity before presenting a token to it                                                                                              | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 5.2    | MUST   | the client validates the TLS certificate chain when presenting a token to a resource server                                                                                     | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 5.3    | MUST   | a client implementation ensures bearer tokens are not leaked to unintended parties                                                                                              | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 5.3    | MUST   | the client validates the TLS certificate chain when requesting protected resources                                                                                              | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 5.3    | MUST   | the client always uses TLS for requests carrying a bearer token                                                                                                                 | —       | n/a: client-side guidance; Odudu is the resource server, not a client                                                          |
| 5.3    | MUST   | an implementation does not store bearer tokens in cookies sent in the clear                                                                                                     | —       | gap                                                                                                                            |
| 5.3    | MUST   | an implementation storing bearer tokens in cookies takes CSRF precautions                                                                                                       | —       | n/a: Odudu's userinfo endpoint reads the token from the `Authorization` header, never from a cookie                            |
| 5.3    | SHOULD | the authorization server issues short-lived (one hour or less) bearer tokens                                                                                                    | —       | gap                                                                                                                            |
| 5.3    | SHOULD | the authorization server issues bearer tokens scoped to an audience restriction                                                                                                 | —       | gap                                                                                                                            |
| 5.3    | SHOULD | bearer tokens are not passed in page URLs                                                                                                                                       | —       | n/a: URI query parameter method dropped in OAuth 2.1; Odudu accepts the `Authorization` header only                            |
