# RFC 7662 — OAuth 2.0 Token Introspection

**Status in Odudu:** in scope for P3b in full, per the design spec's §8.2.
No `/introspect` endpoint exists in this repository yet — this table is
written before the endpoint that resolves its rows, which is this
increment's own first task. What resolves each row is `active` consulting
**session liveness**, not only a grant's `revoked_at`, and scoping a
description to callers entitled through their own `client_id` **or**
through any RFC 8707 resource URI they are registered under — the two
mechanisms §8.2 names, the second corrected from its own literal wording
by "R-27a: the caller's identity" below. §1 (Introduction) is non-normative
background on what introspection is for and is out of scope; §3 (IANA
Considerations) registers the response-name registry itself, not a name in
it, and is not rowed; §6 (References) is never rowed. §2.2's response
members other than `active` — `scope`, `client_id`, `username`,
`token_type`, `exp`, `iat`, `nbf`, `sub`, `aud`, `iss`, `jti` — are
OPTIONAL and get no rows, for the same reason `oidc-discovery.md` gives
OPTIONAL metadata.

## Reading notes

### R-27a: the caller's identity

§8.2's own sentence, read literally, compares the caller's `client_id`
against the token's `aud`. Those are different namespaces: `aud` is built
from `client_oidc_config.audiences`, RFC 8707 resource URIs, while a
`/introspect` caller authenticates with client credentials and is
identified by a `client_id` — and a `client_id` never appears in an `aud`
built from resource URIs, so the sentence taken literally would answer
`{"active": false}` to every caller.

The rule actually implemented: a caller is entitled to a description when
the token's `aud` intersects the caller's own identity, where that
identity is its `client_id` **or** any value in its own registered
`audiences`. A resource server is a client whose `audiences` name the
resources it serves, which is how a deployment expresses "this token is
for me"; the `client_id` half is kept so a deployment that registers
audiences as client ids still works. `IntrospectionCaller`
(`packages/protocol-oidc/src/usecase/introspection.ts`) carries both,
named as its own type so a call site cannot pass a bare string and have
either dimension silently mean nothing.

### R-27d: why the grant needs its own claim

`token_grants` carries no unique constraint over `{client_id, subject_id,
session_id}`, and every issuance path inserts a fresh row — for
`client_credentials`, `subject_id` is the client's stable
`service_subject_id` and `session_id` is always `null`, so every grant
that client will ever hold shares the identical triple. A query over that
triple cannot tell a revoked grant from a live sibling.

So the access token carries a private `grant_id` claim — the
`token_grants` row's own id, generated before the token is minted and
signed into it — and introspection (and, later, `/revoke`, per §8.3's
"against the grant the token names") looks a grant up by that id alone.
See `docs/protocols/rfc9068.md`'s reading note on private claims for how
`grant_id` sits beside `sid`.

### §2.2's fused MUST, and the spike that settles it

A spike ran before this table was written, per `CLAUDE.md`'s P0 rule for a
claim about third-party (specification) behaviour: `assumption:` §2.2
permits answering `{"active": false}` to an authenticated caller not
authorized for the token, rather than requiring an error. The assumption
**held, and more strongly than that phrasing states**. §2.2's final
normative paragraph, verbatim:

> If the introspection call is properly authorized but the token is not
> active, does not exist on this server, or the protected resource is not
> allowed to introspect this particular token, then the authorization
> server MUST return an introspection response with the "active" field set
> to "false". Note that to avoid disclosing too much of the authorization
> server's state to a third party, the authorization server SHOULD NOT
> include any additional information about an inactive token, including
> why the token is inactive.

Two things follow that the brief's own phrasing understates. First,
`{"active": false}` for an authorized caller who simply is not entitled to
the token is **required**, not merely permitted — the design spec's
decision to scope a description by `aud` membership stands, and the
alternative it rejected, an explicit error, would be a conformance
violation rather than a defensible different choice. Second, the sentence
fuses three distinct conditions — token inactive, token unknown, caller not
allowed — into one response on purpose: an introspection response must not
let a caller distinguish them. That MUST is the `2.2` row below headed
"the authorization server responds with `active: false`"; the `SHOULD NOT`
that follows it, about omitting any further detail on an inactive token,
is a second, separate row rather than a footnote on the first.

### §2.3's declarative sentences, already rowed elsewhere

§2.3 is titled "Error Response" but contains exactly one capitalized RFC
2119 keyword — the MUST below, that a query for an inactive, unknown, or
disallowed token is answered per §2.2 rather than as an OAuth error
object. Its other two sentences, describing an HTTP 401 on invalid client
credentials or an insufficient bearer token, are declarative ("...the
authorization server responds with an HTTP 401...") and carry no keyword
of their own. Both restate an obligation already normative — and already
rowed — elsewhere: `docs/protocols/rfc6749.md` §5.2 rows the 401-on-
`invalid_client` behaviour (`RFC6749-5.2-02`), and
`docs/protocols/rfc6750.md` §3.1 rows the 401-on-`invalid_token` behaviour
(`RFC6750-3.1-01`). Rowing them again here, at a level the text does not
carry, would both invent a level and double-count an obligation this
process has already rowed once. Not rowed here.

### TLS on the introspection endpoint

§2 and §4 each carry a MUST that the introspection endpoint runs under
TLS. Odudu terminates no TLS — `docs/protocols/rfc6749.md`'s reading note
"TLS: what a boot guard settles, and what it does not" is the standing
account of what `assertProductionTls` does and does not prove, and both
rows here are `accepted:` against it rather than newly argued.

### Two restatements, rowed once

§4 restates two obligations §2 already states. The first pair is close but
not identical: §2.1's MUST that the endpoint "require some form of
authorization" permits a bearer token as well as client authentication,
while §4's MUST names authentication specifically; they are treated as one
obligation and rowed once, at §2.1, because §4 draws no distinction that
changes what a test would check. The second pair is exact: an
inactive-token response carries nothing beyond `active: false`, stated
once as a `SHOULD NOT` at §2.2 and restated in the same words at §4. Both
are rowed once, at the section that states them first. §4 adds one
genuinely new obligation next to the first restatement — that a protected
resource be **specifically authorized** to call the endpoint, not merely
authenticated — and that gets its own row below.

### `gap` versus `deferred: P3b` for an undecided MAY

An unaddressed MAY is `gap` when nothing in this phase forces a decision on
it — CORS, JSONP, or ignoring `token_type_hint` outright, none of which
building `/introspect` or `/revoke` requires taking a position on. The same
kind of MAY is `deferred: P3b` instead when building the row's own feature
necessarily forces the choice, or an already-committed mechanism exercises
it — the introspection endpoint's own choice of HTTP method, or the two
MAYs above that the audience-scoped response already commits to. This rule
applies the same way in `rfc7009.md` and `rfc8707.md`.

## Clause table

| Clause | Level  | Requirement                                                                                                                                                                                                                          | Test ID | Status                                                                                                                                   |
| ------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 2      | MUST   | the introspection endpoint runs under a transport-layer security mechanism                                                                                                                                                           | —       | accepted: "TLS on the introspection endpoint"                                                                                            |
| 2.1    | MUST   | the introspection endpoint requires the `token` parameter                                                                                                                                                                            | —       | deferred: P3b — no `/introspect` endpoint exists in this repository yet                                                                  |
| 2.1    | MUST   | the authorization server extends its token search across all supported token types when it cannot locate a token using the caller's `token_type_hint`                                                                                | —       | deferred: P3b — same reason                                                                                                              |
| 2.1    | MAY    | the authorization server ignores `token_type_hint`, particularly when it can detect the token type automatically                                                                                                                     | —       | gap                                                                                                                                      |
| 2.1    | SHOULD | the authorization server returns an introspection response indicating the token is not active, rather than an error, when it cannot determine the token's state from the information given                                           | —       | deferred: P3b — same reason                                                                                                              |
| 2.1    | MUST   | the introspection endpoint requires authorization to access it, such as client authentication or a bearer token, to prevent token-scanning attacks                                                                                   | —       | deferred: P3b — same reason                                                                                                              |
| 2.2    | MUST   | the introspection response includes an `active` boolean member                                                                                                                                                                       | —       | deferred: P3b — same reason                                                                                                              |
| 2.2    | MUST   | a response member name intended for cross-domain use is registered in the OAuth Token Introspection Response registry                                                                                                                | —       | n/a: Odudu's introspection response, once built, emits no service-specific extension member for the registry obligation to bind          |
| 2.2    | MAY    | the authorization server responds differently to different protected resources making the same introspection request                                                                                                                 | —       | deferred: P3b — the mechanism design spec §8.2 names as scoping a description to callers whose `client_id` is in the token's `aud`       |
| 2.2    | MAY    | the authorization server limits which scopes from a given token are returned to a given protected resource                                                                                                                           | —       | deferred: P3b — same reason                                                                                                              |
| 2.2    | MUST   | if the introspection call is properly authorized but the token is not active, does not exist on this server, or the caller is not allowed to introspect it, the authorization server responds with `active: false`                   | —       | deferred: P3b — see "§2.2's fused MUST, and the spike that settles it"; no `/introspect` endpoint exists yet                             |
| 2.2    | SHOULD | the authorization server does not include any additional information about an inactive token in the response, including why it is inactive                                                                                           | —       | deferred: P3b — same reason                                                                                                              |
| 2.3    | MUST   | a properly formed and authorized query for an inactive token, an unknown token, or a token the caller is not allowed to know about is not an error response; the authorization server instead responds per §2.2 with `active: false` | —       | deferred: P3b — see "§2.2's fused MUST, and the spike that settles it"; no `/introspect` endpoint exists yet                             |
| 4      | MUST   | the authorization server determines whether an expirable token has expired                                                                                                                                                           | —       | deferred: P3b — same reason                                                                                                              |
| 4      | MUST   | the authorization server determines whether a token whose valid period can start later has reached it                                                                                                                                | —       | n/a: no token Odudu issues carries not-before semantics; every token is valid from the moment it is minted                               |
| 4      | MUST   | the authorization server determines whether a revocable token has been revoked                                                                                                                                                       | —       | deferred: P3b — same reason                                                                                                              |
| 4      | MUST   | the authorization server validates the signature of a signed token                                                                                                                                                                   | —       | deferred: P3b — same reason                                                                                                              |
| 4      | MUST   | the authorization server determines whether a token restricted to certain resource servers can be used at the one making the introspection call                                                                                      | —       | deferred: P3b — same reason                                                                                                              |
| 4      | SHOULD | the authorization server requires a protected resource to be specifically authorized to call the introspection endpoint, not merely authenticated                                                                                    | —       | deferred: P3b — same reason                                                                                                              |
| 4      | MUST   | the authorization server supports TLS 1.2                                                                                                                                                                                            | —       | accepted: "TLS on the introspection endpoint"                                                                                            |
| 4      | MAY    | the authorization server disallows HTTP GET on the introspection endpoint, requiring POST, to keep token values out of server-side logs via query parameters                                                                         | —       | deferred: P3b — same reason                                                                                                              |
| 4      | MUST   | the party presenting a token for introspection performs a TLS server certificate check when using TLS                                                                                                                                | —       | n/a: client-side/protected-resource-side guidance; Odudu is the server end of this connection, not the party validating a certificate    |
| 4      | MUST   | the designers of a system using this protocol consider the performance and security trade-offs of caching introspection responses                                                                                                    | —       | n/a: directed at the humans designing a deployment, not a behaviour this server exhibits                                                 |
| 4      | MUST   | the introspection response is not cached beyond the time indicated in its `exp` member, when present                                                                                                                                 | —       | n/a: caching is the protected resource's own choice; the obligation binds the party consuming the response, not the authorization server |
| 5      | MUST   | measures are taken to prevent disclosure of privacy-sensitive introspection-response information, such as resource-owner identifiers, to unintended parties                                                                          | —       | deferred: P3b — same reason                                                                                                              |
