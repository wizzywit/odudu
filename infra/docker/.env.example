# Consumed by infra/docker/compose.yaml. Copy to infra/docker/.env — these
# are local-development placeholders, not secrets to guard.
POSTGRES_USER=odudu
POSTGRES_PASSWORD=odudu
POSTGRES_DB=odudu
ODUDU_SVC_PASSWORD=odudu_svc
